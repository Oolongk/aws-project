---
title: "Tạo Lambda Reader API"
date: 2026-07-06
weight: 43
chapter: false
pre: "4.3"
---

Lambda Reader là function riêng với Lambda QMJ Loader. Reader nhận `ticker` từ API Gateway, query DynamoDB theo Partition Key và trả historical QMJ ở JSON.

| Bước | Hành động | Chi tiết kỹ thuật |
|---|---|---|
| 1 | AWS Lambda → Functions → **Create function** | Chọn **Author from scratch** |
| 2 | Function name | Nhập `stock-screener-reader` |
| 3 | Runtime | Chọn Python runtime đang được hỗ trợ trong Console |
| 4 | Execution role | Chọn **Use an existing role** → `LambdaStockScreenerReaderRole` |
| 5 | Chọn **Create function** | Mở Code tab |
| 6 | Dán source bên dưới | Function này không đọc S3 và không chạy awswrangler |
| 7 | Configuration → Environment variables | Thêm `DYNAMODB_TABLE=qmj_screener` |
| 8 | Chọn **Deploy** | Lưu code |

## Lambda source code

````python
import json
import os
from decimal import Decimal

import boto3
from boto3.dynamodb.conditions import Key

TABLE_NAME = os.environ["DYNAMODB_TABLE"]
table = boto3.resource("dynamodb").Table(TABLE_NAME)


def json_default(value):
    # DynamoDB trả Decimal; API response cần JSON number.
    if isinstance(value, Decimal):
        return float(value)
    raise TypeError(f"Unsupported type: {type(value)}")


def response(status_code, body):
    return {
        "statusCode": status_code,
        "headers": {
            "Content-Type": "application/json",
            "Access-Control-Allow-Origin": "*",
            "Access-Control-Allow-Methods": "GET,OPTIONS",
            "Access-Control-Allow-Headers": "Content-Type"
        },
        "body": json.dumps(body, default=json_default)
    }


def lambda_handler(event, context):
    params = event.get("queryStringParameters") or {}
    ticker = (params.get("ticker") or "").strip().upper()

    if not ticker:
        return response(400, {"message": "ticker is required"})

    result = table.query(
        KeyConditionExpression=Key("ticker").eq(ticker),
        ScanIndexForward=True
    )

    return response(200, {
        "ticker": ticker,
        "items": result.get("Items", [])
    })
````

> **Tư duy kiến trúc sư – query not scan.** Reader chỉ có quyền `dynamodb:Query`. Không cấp `dynamodb:Scan`, không cần S3 permission và không dùng Loader Role.
